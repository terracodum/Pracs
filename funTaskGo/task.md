package main

// =============================================================================
// GO IDIOMS — ЗАДАЧКИ ДЛЯ ПРОКАЧКИ
// решай в любом порядке, меняй только тело функции
// =============================================================================

// -----------------------------------------------------------------------------
// 1. УНИКАЛЬНЫЕ ЭЛЕМЕНТЫ ✓
// Вернуть срез без дубликатов, сохранить порядок
// -----------------------------------------------------------------------------
// func unique(input []string) []string

// -----------------------------------------------------------------------------
// 2. ГРУППИРОВКА ПО ПЕРВОЙ БУКВЕ
// Сгруппировать слова по первой букве
// unique(["apple", "banana", "avocado", "cherry"]) ->
//   {"a": ["apple", "avocado"], "b": ["banana"], "c": ["cherry"]}
// -----------------------------------------------------------------------------
func groupByFirstLetter(input []string) map[string][]string {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 3. ПОДСЧЁТ ВХОЖДЕНИЙ
// Посчитать сколько раз каждое слово встречается
// count(["a", "b", "a", "c", "b", "a"]) -> {"a": 3, "b": 2, "c": 1}
// -----------------------------------------------------------------------------
func wordCount(input []string) map[string]int {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 4. РАЗВЕРНУТЬ СРЕЗ
// Вернуть новый срез в обратном порядке (без slices.Reverse)
// reverse([1, 2, 3, 4, 5]) -> [5, 4, 3, 2, 1]
// -----------------------------------------------------------------------------
func reverse(input []int) []int {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 5. ПЕРЕСЕЧЕНИЕ ДВУХ СРЕЗОВ
// Вернуть элементы которые есть в обоих срезах (без дубликатов)
// intersect([1,2,3,4], [2,4,6]) -> [2, 4]
// -----------------------------------------------------------------------------
func intersect(a, b []int) []int {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 6. ФИЛЬТРАЦИЯ СТРУКТУР
// Вернуть только активных пользователей
// -----------------------------------------------------------------------------
type User struct {
	Name   string
	Age    int
	Active bool
}

func filterActive(users []User) []User {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 7. СОРТИРОВКА СТРУКТУР ПО ПОЛЮ
// Отсортировать пользователей по возрасту (по возрастанию)
// подсказка: sort.Slice
// -----------------------------------------------------------------------------
func sortByAge(users []User) []User {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 8. МАКСИМУМ ПО ПОЛЮ
// Найти пользователя с максимальным возрастом
// если срез пустой — вернуть ошибку
// -----------------------------------------------------------------------------
func oldest(users []User) (User, error) {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 9. ПАЛИНДРОМ
// Проверить является ли строка палиндромом
// isPalindrome("racecar") -> true
// isPalindrome("hello")   -> false
// -----------------------------------------------------------------------------
func isPalindrome(s string) bool {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 10. RLE СЖАТИЕ
// Сжать строку методом run-length encoding
// compress("aaabbc") -> "a3b2c1"
// -----------------------------------------------------------------------------
func compress(s string) string {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 11. СВОЯ ОШИБКА
// Написать тип ValidationError с полями Field и Message
// функция validateAge возвращает ValidationError если age < 0 или age > 150
// -----------------------------------------------------------------------------
type ValidationError struct {
	panic("implement me")
}

func (e *ValidationError) Error() string {
	panic("implement me")
}

func validateAge(age int) error {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 12. ГОРУТИНЫ + КАНАЛ
// Запустить 5 горутин, каждая возвращает свой индекс * 2
// собрать все результаты в срез (порядок не важен)
// -----------------------------------------------------------------------------
func runWorkers() []int {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 13. ТАЙМАУТ
// Сделать функцию которая пытается получить значение из канала
// если за 1 секунду ничего не пришло — вернуть ошибку "timeout"
// подсказка: select + time.After
// -----------------------------------------------------------------------------
func withTimeout(ch <-chan int) (int, error) {
	panic("implement me")
}

// -----------------------------------------------------------------------------
// 14. WORKER POOL
// N воркеров обрабатывают jobs из канала
// каждый воркер умножает число на 2 и пишет в results
// дождаться всех через WaitGroup
// -----------------------------------------------------------------------------
func workerPool(jobs []int, numWorkers int) []int {
	panic("implement me")
}

func main() {
	// тест здесь
}